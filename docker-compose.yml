version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flask_frontend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DEBUG=False
      - BACKEND_URL=http://localhost:3000 
      - SECRET_KEY=clave_secreta_ep2_super_segura
    restart: always